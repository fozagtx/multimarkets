[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.agents"
watchPatterns = ["packages/agents/**", "packages/contracts/**", "pnpm-lock.yaml"]

[deploy]
healthcheckPath = "/ready"
healthcheckTimeout = 90
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
